# Incident Manager

This is the Incident Manager microservice that manages incidents.

## Running

Incident Manager requires Redis and Pub/Sub. See API and Logger on how to set them up.

To run the locally, use the following command:

```bash
go run .
```