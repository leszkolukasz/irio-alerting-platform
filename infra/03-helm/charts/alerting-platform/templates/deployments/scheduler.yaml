apiVersion: apps/v1
kind: Deployment
metadata:
  name: alerting-platform-scheduler
  labels:
    app: alerting-platform-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alerting-platform-scheduler
  template:
    metadata:
      labels:
        app: alerting-platform-scheduler
    spec:
      containers:
        - name: alerting-platform-scheduler
          image: "{{ .Values.gcloud.registryURL }}/alerting-platform-scheduler:{{ .Values.env.VERSION }}"
          imagePullPolicy: Always
          env:
          {{- range $key, $value := .Values.env }}
            - name: {{ $key | quote }}
              value: {{ $value | required (print "Missing value: " $key) | quote }}
          {{- end }}

          {{- range $key, $value := .Values.secrets }}
            - name: {{ $key | quote }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: {{ $key | quote }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /live
              port: {{ .Values.env.LIVE_PORT }}
            initialDelaySeconds: 60
            periodSeconds: 10
