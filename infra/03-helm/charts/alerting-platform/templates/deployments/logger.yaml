apiVersion: apps/v1
kind: Deployment
metadata:
  name: alerting-platform-logger
  labels:
    app: alerting-platform-logger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alerting-platform-logger
  template:
    metadata:
      labels:
        app: alerting-platform-logger
    spec:
      containers:
        - name: alerting-platform-logger
          image: "{{ .Values.gcloud.registryURL }}/alerting-platform-logger:{{ .Values.env.VERSION }}"
          imagePullPolicy: Always
          env:
          {{- range $key, $value := .Values.env }}
            - name: {{ $key | quote }}
              value: {{ $value | required (print "Missing value: " $key) | quote }}
          {{- end }}

          {{- range $key, $value := .Values.secrets }}
            - name: {{ $key | quote }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: {{ $key | quote }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /live
              port: {{ .Values.env.LIVE_PORT }}
            initialDelaySeconds: 60
            periodSeconds: 10
